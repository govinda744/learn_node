// impor model
// const abc = require('./../models/user.model')
function insert(data) {
// abc.save();
// save will either return promise or return your own promise

}

function find(condition) {
  // abc.find()
}

function update(id, data) {

}

function remove(id) {

}

module.export = {
  insert, find, update, remove
}